name: CI

on:
  pull_request:
    branches:
      - '*'

jobs:
  test-backend:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.8]

    services:
      postgres:
        image: postgres:11.5
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        # needed because the postgres container does not provide a healthcheck
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5

    steps:
    - uses: actions/checkout@v1
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}
    - uses: actions/cache@v1
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}-${{ hashFiles('**/dev-requirements.txt') }}
    - name: Install dependencies
      run: |
        sudo apt-get install libpq-dev  # required b/c it has pg_config which is needed to build psycopg2
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r dev-requirements.txt
    - name: Test with pytest
      env:
        SLACK_API_TOKEN: ""
        DATABASE_URL: "postgres://postgres:postgres@localhost:5432/postgres"
        DJANGO_SETTINGS_MODULE: "penny_university.settings.ci"
        SECRET_KEY: "6db7242adc5570c3bb2d12b2e161b1930650f9669a457fb3"
      run: |
        pytest
    - name: Flake8
      run: |
        flake8

  test-frontend:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [ '12' ]
    steps:
    - uses: actions/checkout@v1
    - name: Set Up Node ${{ matrix.node }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node }}
    # TODO add dependency caching here
    - name: Install Dependencies
      working-directory: ./penny_university_frontend
      run: |
          npm install
    - name: Run Tests
      working-directory: ./penny_university_frontend
      run: |
        CI=true npm test
    - name: Eslint
      working-directory: ./penny_university_frontend
      run: |
        npm run lint
    - name: Build Frontend
      # this broke deployment once, so better check
      working-directory: ./penny_university_frontend
      env:
        CI: false
        REACT_APP_API_ROOT: "/api/"
      run: |
        npm install
        npm run build
  
  test-integration:
    runs-on: ubuntu-16.04
    strategy:
      matrix:
        node: [ '12' ]
    steps:
      - uses: actions/checkout@v1
      - name: Add Heroku credentials
        run: |
          echo "machine api.heroku.com" > ~/.netrc
          echo "  login penny.university.mod@gmail.com" >> ~/.netrc
          echo "  password ${{ secrets.HEROKU_API_KEYs }}" >> ~/.netrc
          echo "machine git.heroku.com" >> ~/.netrc
          echo "  login penny.university.mod@gmail.com" >> ~/.netrc
          echo "  password ${{ secrets.HEROKU_API_KEY }}" >> ~/.netrc
      - name: Deploy to QA
        run: |
          git remote add qa https://git.heroku.com/penny-qa.git
          ./script/deploy_to_qa
      - name: Install Cypress
        working-directory: ./penny_university
        run: |
          npm install
      - name: Cypress Run
        working-directory: ./penny_university
        uses: cypress-io/github-action@v2
        env:
          INTEGRATION: |
            COOKIE_B=.azmcw4cvyxph1qzhlbsxu8gu0
            COOKIE_DS=1608437298
            COOKIE_D=iBbiv1a2Ixu14A7O7CeranbsfNmGX%2FcWQmzUAJSVrIASzxy6LJpkLVSzbeVrn4cztStibH7N7Ia5KAHpTvTmw%2FeoTol8LVMuJiYp7eE5ftiRdYZmIjJS8YAfJU3J9UAL%2BYbZZo3Hb0kGbq1aS2p8AOedMe7BMcp5zg78bQuHG7TNnCmvb9z2G1JowA%3D%3D
            COOKIE_IAP2=treatment
            COOKIE_OPTANONCONSENT=isIABGlobal=false&datestamp=Sat+Dec+19+2020+22%3A09%3A36+GMT-0600+(Central+Standard+Time)&version=6.10.0&hosts=&consentId=176e7236-c06f-4963-b9ec-2ff0628f3bf7&interactionCount=1&landingPath=NotLandingPage&groups=C0004%3A0%2CC0002%3A1%2CC0003%3A1%2CC0001%3A1&AwaitingReconsent=false
            COOKIE_X=azmcw4cvyxph1qzhlbsxu8gu0.1608437123
        with:
          project: ./tests/integration

const getChats = (state) => state.entities.chats

const getFollowUps = (state) => state.entities.followUps

const getUserProfiles = (state) => state.entities.userProfiles


export {
  getChats,
  getFollowUps,
  getUserProfiles,
}